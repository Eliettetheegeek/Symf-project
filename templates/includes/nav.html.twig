<div class="navbar">
    <div class="container flex">
        <h1 class="logo"><a href="{{ path('app_home')}}">Blue Bird</a></h1>
        <nav>
            <ul>
                    <li><a href="{{ path('app_home')}}">Accueil</a></li>
                    <li><a href="{{ path('app_product_index') }}">Produits</a></li>
                    <li><a href="{{ path('app_panier') }}">Panier</a></li>
                    
                 {% if app.user %}
                    <li><a href="{{ path('app_product_new')}}">Nouveau produit</a></li>
                    <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                    <li>Hello <strong>{{ app.user.firstname }}</strong></li>
                 {% else %}
                    <li><a href="{{ path('app_login')}}">Connexion</a></li>
                {% endif %}
            </ul>
        </nav>
    </div>
</div>